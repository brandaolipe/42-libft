ft_atoi(const char *nptr)
{
	int	signal;

	signal = 1;
	while (*nptr = ' ')
		nptr++;
	if (*nptr = '-')
	{
		signal *= -1;
		nptr++;
	}
	while (*nptr <= '9' && *nptr >= '0')
	{
		number = (number * 10) + (
	}

}
